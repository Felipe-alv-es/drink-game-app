export const getQuestionButtonStyle = (color?: string) => ({
  width: "100px",
  textTransform: "none",
  borderRadius: "16px",
  borderColor: color ? color : "#4c4c4c",
  color: color ? color : "#4c4c4c",
});
